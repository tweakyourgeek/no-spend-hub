/**
 * Re-export the context hooks for convenience.
 * Components should import from here, not directly from contexts.
 */
export { useGameState, useGameDispatch } from '../contexts/GameStateContext';
