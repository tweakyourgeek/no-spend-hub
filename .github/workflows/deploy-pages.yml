name: Deploy Sub-Apps to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'src/features/**'
      - 'nospendlandia/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-deploy
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Restore cached builds
        uses: actions/cache@v4
        with:
          path: _site
          key: pages-builds-${{ github.sha }}
          restore-keys: |
            pages-builds-

      - name: Detect changed apps
        id: detect
        run: |
          BUILD_ALL=false
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            BUILD_ALL=true
          elif ! git rev-parse HEAD~1 >/dev/null 2>&1; then
            BUILD_ALL=true
          elif git diff --name-only HEAD~1 HEAD | grep -q '.github/workflows/deploy-pages.yml'; then
            BUILD_ALL=true
          fi

          changed=""
          # Check sub-apps in src/features/
          for dir in src/features/*/; do
            app=$(basename "$dir")
            if [ -f "$dir/vite.config.js" ] || [ -f "$dir/vite.config.ts" ]; then
              if [ "$BUILD_ALL" = "true" ] || git diff --name-only HEAD~1 HEAD | grep -q "^src/features/$app/"; then
                changed="$changed src/features/$app"
              fi
            fi
          done
          # Check top-level apps
          for dir in nospendlandia; do
            if [ -d "$dir" ] && { [ -f "$dir/vite.config.js" ] || [ -f "$dir/vite.config.ts" ]; }; then
              if [ "$BUILD_ALL" = "true" ] || git diff --name-only HEAD~1 HEAD | grep -q "^$dir/"; then
                changed="$changed $dir"
              fi
            fi
          done

          echo "apps=${changed# }" >> $GITHUB_OUTPUT
          if [ -n "${changed# }" ]; then
            echo "Apps to build:${changed}"
          else
            echo "No apps changed"
          fi

      - name: Build apps
        run: |
          mkdir -p _site
          changed="${{ steps.detect.outputs.apps }}"

          # Collect all app dirs: src/features/* and top-level nospendlandia
          app_dirs=""
          for dir in src/features/*/; do
            [ -f "$dir/vite.config.js" ] || [ -f "$dir/vite.config.ts" ] && app_dirs="$app_dirs $dir"
          done
          for dir in nospendlandia; do
            [ -d "$dir" ] && { [ -f "$dir/vite.config.js" ] || [ -f "$dir/vite.config.ts" ]; } && app_dirs="$app_dirs $dir"
          done

          REPO_ROOT="$PWD"
          for dir in $app_dirs; do
            dir="${dir%/}"
            app=$(basename "$dir")
            # Build if: app changed, or not yet in cache
            if echo " $changed " | grep -q " $dir " || [ ! -d "_site/$app" ]; then
              echo "::group::Building $app"
              cd "$dir"
              if [ -f package-lock.json ]; then npm ci; else npm install; fi
              npm run build
              rm -rf "$REPO_ROOT/_site/$app"
              cp -r dist "$REPO_ROOT/_site/$app"
              cd "$REPO_ROOT"
              echo "::endgroup::"
            else
              echo "Skipping $app (cached, unchanged)"
            fi
          done

      - name: Generate index page
        run: |
          cat > _site/index.html << 'INDEXEOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8" />
            <meta name="viewport" content="width=device-width, initial-scale=1.0" />
            <title>No Spend Hub — Apps</title>
            <style>
              * { margin: 0; padding: 0; box-sizing: border-box; }
              body {
                font-family: 'Open Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
                background: #0a0a0a; color: #e0e0e0;
                min-height: 100vh; padding: 3rem 1.5rem;
              }
              .container { max-width: 720px; margin: 0 auto; }
              h1 { font-family: 'Cormorant Garamond', Georgia, serif; font-size: 2rem; margin-bottom: 0.5rem; color: #fff; }
              .subtitle { color: #888; margin-bottom: 2rem; font-size: 0.95rem; }
              .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 0.75rem; }
              a {
                display: block; padding: 1rem 1.25rem;
                background: #1a1a1a; border: 1px solid #2a2a2a; border-radius: 8px;
                color: #e0e0e0; text-decoration: none;
                transition: border-color 0.2s, background 0.2s;
              }
              a:hover { border-color: #555; background: #222; }
              .app-name { font-weight: 600; font-size: 0.9rem; }
            </style>
          </head>
          <body>
            <div class="container">
              <h1>No Spend Hub</h1>
              <p class="subtitle">Sub-app directory</p>
              <div class="grid">
          INDEXEOF

          for dir in src/features/*/ nospendlandia; do
            app=$(basename "$dir")
            if [ -d "$dir" ] && { [ -f "$dir/vite.config.js" ] || [ -f "$dir/vite.config.ts" ]; }; then
              label=$(echo "$app" | sed 's/-/ /g; s/\b\(.\)/\u\1/g')
              echo "        <a href=\"/no-spend-hub/$app/\"><span class=\"app-name\">$label</span></a>" >> _site/index.html
            fi
          done

          cat >> _site/index.html << 'INDEXEOF'
              </div>
            </div>
          </body>
          </html>
          INDEXEOF

          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
